/* eslint-disable import/extensions */
import { probabilitySelectingPerson } from "./functions.js";

test("check", () => {
  expect(probabilitySelectingPerson(1, 2, 1, 2)).toBe(
    "1 / 2 in fraction and 0.5000 decimal"
  );
});

test("check2", () => {
  expect(probabilitySelectingPerson(1, 2, 1, 2)).toBe(
    "1 / 2 in fraction and 0.5000 decimal"
  );
});

test("check3", () => {
  expect(probabilitySelectingPerson(1, 2, 1, 2)).toBe(
    "1 / 2 in fraction and 0.5000 decimal"
  );
});
